// import { fromJS } from 'immutable';
// import { makeSelectCorpusDomain } from '../selectors';

// const selector = makeSelectCorpusDomain();

describe('makeSelectCorpusDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
